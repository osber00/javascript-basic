const nombre = 'Oscar'
const apellido = 'Bertel'

const nombreCompleto = `${nombre} ${apellido}`

console.log(nombreCompleto);

const saludo = (nombre)=> (
    `Hola ${nombre}`
)

console.log(saludo('Mary'));